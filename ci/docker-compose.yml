version: "2"

services:
  db:
    image: mariadb:10.0.30
    container_name: mysql
    ports:
      - "3306:3306"
    environment:
      MYSQL_DATABASE: cfptest
      MYSQL_ALLOW_EMPTY_PASSWORD: "yes"